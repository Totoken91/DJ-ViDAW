{
  "name": "dj-vidaw",
  "version": "1.0.0",
  "private": true,
  "description": "DJ ViDAW - le DAW le plus goofy du web. Par DJ Viteau.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "build:artifact": "npm run build && node scripts/build-artifact.mjs dist/artifact.html"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
